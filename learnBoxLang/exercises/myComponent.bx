class {
    function multiplyTheNumbers(
            required numeric firstnum,
            required numeric secondnum,
            required numeric thirdnum) {
        return firstnum * secondnum * thirdnum;
    }
}
